# ============================================
# FeedWise Docker 部署配置
# 复制此文件为 .env 并填写实际值
# ============================================

# ---------- FreshRSS 配置 (必填) ----------
FRESHRSS_URL=https://your-freshrss.example.com
FRESHRSS_USERNAME=your_username
FRESHRSS_API_PASSWORD=your_api_password

# ---------- LLM 配置 ----------
# 选择 LLM 提供商: openai 或 ollama
LLM_PROVIDER=openai

# OpenAI 配置 (LLM_PROVIDER=openai 时使用)
OPENAI_API_KEY=sk-your-api-key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Ollama 配置 (LLM_PROVIDER=ollama 时使用)
# 注意: Docker 容器访问宿主机需使用特殊地址
# Linux: http://172.17.0.1:11434 或 http://host.docker.internal:11434
# macOS/Windows: http://host.docker.internal:11434
OLLAMA_HOST=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2

# ---------- 同步配置 ----------
SYNC_INTERVAL_MINUTES=30

# ---------- 前端配置 ----------
# 前端访问后端的地址 (构建时使用)
# 设置为用户浏览器能访问到的后端地址
# 例如: http://192.168.1.100:8000 或 http://your-server:8000
VITE_API_BASE=http://localhost:8000
